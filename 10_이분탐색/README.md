이분탐색법

배열의 크기를 절반으로 줄이면서 답을 찾는 알고리즘
주로 반복문으로 구현
시간 복잡도 O(logn) 
N이 굉장히 클때, 굉장히 빨리 계산할 때 사용
-> 알고리즘 성립을 위해서는 배열이 반드시 정렬되어 있어야함

이분 탐색의 대상이 되는 원소들을 트리에 넣으면 이진탐색트리가 됨

10815

라이브 코딩 10816과 유사한 문제 10816은 가지고있는 카드의 개수를 물어봤다면 10815는 입력된 숫자가 적힌 숫자 카드를 가지고 있는지를 물어봄.그래서 binarySearch함수를 사용해 같은 숫자가 배열에 존재하는지 확인함.

16401

처음에는 과자의 수가 아이들의 수보다 많으면 길이가 긴 과자가 mid부터 right까지의 과자수가 m만큼 남는 경우를 찾으려 했으나 이는 하나의 과자를 같은 길이로 쪼갰을때 길이가 maxLen보다 길거나 같은게 두개 이상 나오는 경우를 찾지 못하므로 틀린방식이였다. 그래서 i번째 과자를 mid길이로 쪼개서 cnt에 저장했을때로 저장했을때 cnt가 m보다 크거나 같으면 그 길이가 maxLen이 되도록 했다.

투포인터

누적합: 각 칸마다 해당 칸까지의 합을 저장

두개의 포인터 사이의 거리가 고정된다면 슬라이딩 윈도우 사용

투포인터 사용 경우
1. 2개의 포인터가 다른 위치에서 시작하여 서로에게 다가가는 방향으로 탐색
2. 2개의 포인터가 같은 위치에서 시작하여 같은 방향으로 이동하며 탐색

1의 경우 일반적으로 배열이 정렬됐을때만 성립

